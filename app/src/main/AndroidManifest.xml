<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApplicationMobileAirVip"
        tools:targetApi="31">


    <activity android:name=".view.HomeActivity" />
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.FlightStatusActivity" />
        <activity android:name=".view.TripsActivity" />
        <activity android:name=".view.SettingsActivity" />
        <activity android:name=".view.CompteActivity" />
        <activity android:name=".view.VolListActivity" />
        <activity android:name=".view.ConfirmationActivity" />
        <activity android:name=".view.BillingActivity" />
        <activity android:name=".view.PaiementActivity" />

        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>